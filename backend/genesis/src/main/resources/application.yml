spring:
  threads:
    virtual:
      enabled: true
  data:
    redis:
      host: localhost
      port: 6379
      # Para alta carga, aumentamos o pool de conexões (Lettuce)
      lettuce:
        pool:
          max-active: 100
          max-idle: 50
          min-idle: 10
server:
  port: 8080
